# Server
PORT=3000
NODE_ENV=development

# Session secret (change this to a long random string in production)
SESSION_SECRET=change-this-to-a-secure-random-string

# Default admin account (created on first run)
ADMIN_EMAIL=admin@statech.com
ADMIN_PASSWORD=changeme123

# Additional team members (comma-separated emails allowed to register via admin)
# Only the default admin is created automatically; add more users through the admin panel
